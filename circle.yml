machine:
  services:
    - docker

checkout:
  post:
    - git submodule update --init --recursive

dependencies:
  override:
    - docker info

test:
  override:
    - docker build -t dudlab/nanshe_workflow .
